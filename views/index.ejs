<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to School System</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            background-color: #f8f9fa;
        }

        .welcome-container {
            text-align: center;
            padding: 30px;
            border: 1px solid #ddd;
            border-radius: 10px;
            background-color: #ffffff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>

<body>
    <div class="welcome-container">
        <div>
            <img style="background: red;" class="logo" alt="Smart School Manager" src="image.png" height="60"
                width="191">
        </div>
        <h1 class="display-4">Welcome to the School System</h1>
        <p class="lead">Your one-stop solution for managing school activities</p>
        <hr class="my-4">
        <p>Get started by exploring our features and functionalities.</p>
        <a class="btn btn-primary btn-lg" href="/dashboard" role="button">Learn more</a>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script>

        const verifyToken = () => {
            const token = localStorage.getItem('token');
            if (token) {
                return true
            } else {
                return false;
            }
        }

        if (!verifyToken()) {
            window.location.href = '/login';
        }


    </script>
</body>

</html>